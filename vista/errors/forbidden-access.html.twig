{# empty Twig template #}
{% extends "index.html.twig" %}

{% block content %}
    <div class="cuerpoCrud">
        <div class="contenedor colorCrud">

            <h1 class="text-center">No tienes los permisos necesarios para acceder a esta página.</h1>

            {% if mensaje != null %}
                <h2 id="mensaje">{{ mensaje }}</h2>

            {% endif %}
            <img class="img-fluid d-block" src="/img/403.png">
            <a class="boton-permissions text-white"
               href="index.php?c={{ constant('Utils\\Constantes::HOME_CONTROLLER') }}">Volver al inicio</a>
        </div>
    </div>

{% endblock %}

